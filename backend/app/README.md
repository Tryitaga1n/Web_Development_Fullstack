# API application code

This directory contains the route, controller, model and helper layers used by `server.js`.

- `routes/` maps API paths to controllers.
- `controllers/` validates requests and creates HTTP responses.
- `models/` contains parameterised SQLite operations.
- `lib/` contains authentication, validation, profanity filtering and shared helpers.
